enum SharedEnum {
  deviceId,
  deviceType,
  firebaseToken,
  refreshToken,
  userToken,
  userName,
  userId,
  password,
  userNameLogin,
  theme, // 0 light, 1 dark -- boolean check
  rememberMe,
}
