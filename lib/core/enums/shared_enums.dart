enum SharedEnum {
  deviceId,
  deviceType,
  firebaseToken,
  refreshToken,
  userToken,
  userName,
  userId,
  password,
  userNameLogin,
  deviceModel,
  ipAdress,
  osVersion,
  appVersion,
  theme, // 0 light, 1 dark -- boolean check
}
